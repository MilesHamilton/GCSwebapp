# Why Log

One line per nontrivial decision. Add a row when we lock a decision (loop step 6).

| Date | Decision | Rationale | Alternative rejected | Revisit trigger |
|------|----------|-----------|----------------------|-----------------|
| 2026-07-03 | Basemap = **Mapbox GL JS** | Canonical `MapboxOverlay` story recognizable in GIS interviews; nicer default styles | **MapLibre GL JS** (free, no token) | Token/billing friction, or making the portfolio repo fully OSS/clone-and-run |
| 2026-07-03 | Frontend = **Vite + React + TS** | Fast dev server, standard portfolio stack | CRA (deprecated); Next.js (SSR overkill for a client-side map) | Need SSR, routing, or an API layer colocated with the web app |
| 2026-07-03 | Backend = **FastAPI + uvicorn** | First-class WebSocket + pydantic validation, minimal boilerplate | Flask/Socket.IO; raw `websockets` | Need horizontal scale → add Redis pub/sub |
| 2026-07-03 | Repo = **plain `apps/api` + `apps/web`**, one git repo | Two runtimes don't share a package manager; workspace tooling is overhead | pnpm workspaces / turborepo | Add a shared TS package or a third app |
| 2026-07-03 | deck.gl attach = **`MapboxOverlay` under `react-map-gl`** | Keeps layer updates off React's render cycle for the telemetry hot path | `<DeckGL>` React component (re-renders on every change) | Perf is a non-issue and we want simpler declarative code |
| 2026-07-03 | deck compositing = **overlaid** (`interleaved:false`) | v1 doesn't need deck objects interleaved with map labels/3D | interleaved | want geozones beneath labels or 3D-building occlusion |
| 2026-07-03 | Vehicle marker = **RQ-180 `SimpleMeshLayer`** (2D icon abandoned) | mesh renders reliably; `IconLayer` came up blank (SVG + canvas-PNG both; suspect `billboard:false`) | 2D `IconLayer` | fix the icon, or move to `ScenegraphLayer` |
| 2026-07-03 | 3D mesh zoom-flip **left unresolved (pinned)** | deck #5147: mesh flips orientation at high zoom via MapboxOverlay; not worth blocking v1 | keep debugging / swap layer now | **`ScenegraphLayer` + glTF** in v2 |
| 2026-07-03 | Hot-lane store = **Zustand** | external store read imperatively via `getState()` off React's render cycle; selectors for cold UI | React Context (re-renders all consumers); Redux (boilerplate) | — |
| 2026-07-03 | Live trail = **`PathLayer`** | simplest layer that shows the breadcrumb; store already keeps timestamped points so the data is replay-ready regardless | `TripsLayer` now | Phase 4 replay → swap to `TripsLayer` (one-line factory change) |
| 2026-07-03 | Hot path = **imperative rAF loop + `overlay.setProps`** (read store via `getState`) | pushes telemetry to deck off React entirely; decouples 10 Hz data from ~60 fps draw through the shared store | declarative: subscribe to the store and re-render React each tick | low-rate/simple updates where a React subscription is fine |
| 2026-07-04 | `/ws` = **one send loop per connection** | stateless sim means each client independently derives the same world from `t`, so no shared state or broadcast machinery is needed — simplest thing that works | central **connection manager** holding a client set + broadcast | many clients / broadcast fan-out, presence, backpressure — or the **stateful shared sim in Milestone 2** (senders then read one shared world) |
| 2026-07-04 | Client boundary validation = **TS type assertion** (`as` on parsed JSON) | we own both ends of the contract and pydantic already validates server-side; zero runtime deps | **`zod`** runtime schema parse at the client boundary | an untrusted/third-party feed, or silent wire-drift bugs slip through → add a `zod` parse |
| 2026-07-04 | Geozones = **served in the `snapshot`** from the backend | backend owns all world state (incl. geozones) as one source of truth; the client stays dumb and renders what it's told | **hardcoded client-side** geozone constants | geozones become user-editable / mission-defined → round-trip via the `mission` message |
| 2026-07-04 | Sim = **stateful `VehicleSim.step(dt)`** (integrates state forward) | a command mutates held state and the next steps diverge onto a new path; "turn at ≤ max rate, then hold" has no closed form | closed-form `telemetry_at(t)` (position as a pure `f(t)`) | need to bolt commands onto `f(t)` (can't) — already the reason we moved |
| 2026-07-04 | **One shared sim on one background clock** (`/ws` only reads) | stateless gave a consistent world for free; a stateful sim diverges per-connection → different worlds + command-authority ambiguity; also keeps flying with 0 clients | a `VehicleSim` **per `/ws` connection** | multi-vehicle / distributed sim → authoritative sim service or shared store |
| 2026-07-04 | Dynamics = **3-DOF point-mass**, limits as **clamps**; attitude **derived** (roll from turn rate) | believable map track that respects performance limits, no aero data; derived attitude needs no extra state / no drift | 6-DOF rigid body + aero + inner attitude loop | need real flight-dynamics fidelity (stall, wind, control surfaces) |
| 2026-07-04 | **Physical lap time** — cruise 42 m/s now drives motion (~333 s lap) | speed is real, not cosmetic; the old 20 s lap implied ~700 m/s | keep the old **20 s lap** (dishonest speed) | demo too slow → shrink `LOITER_RADIUS_M` (~400 m floor) or raise cruise within 30–60 |
| 2026-07-04 | Loiter = **feedback guidance** (tangent + radius-capture) reusing the heading loop | captures & holds the ring from any start, Euler-stable, and is the same primitive Phase 7 reuses | closed-form circle / constant turn-rate `ω=V/R` (no capture, drifts) | racetrack CAP → add legs + a small state machine |
| 2026-07-04 | **Fixed `dt`** paced by wall-clock sleep | deterministic dynamics; Euler bounds stay valid; a stall can't cause a large integration jump | variable `dt` from measured wall-clock deltas | need faster-than-real-time or a decoupled sim clock (lockstep) |
| 2026-07-04 | Uplink = new **`command`/`commandAck` on the existing `/ws`**, made full-duplex (two coroutines) | one bidirectional datalink like a real GCS↔autopilot; keeps command + telemetry + ack on one lifecycle | a REST `POST /command`, or a second control socket | need request/reply semantics, external callers, or HTTP caching |
| 2026-07-04 | Command schema = **typed discriminated union** (`kind`; nullable = leave unchanged; `cw`/`ccw` enum) | self-documenting + pydantic/TS-validated; each field still maps to a MAVLink primitive | re-encode MAVLink `COMMAND_LONG` (`param1..7` + `type_mask`) | need real MAVLink interop |
| 2026-07-04 | **Server-authoritative**: client sends *intent* (setpoints), server owns *truth* | client can't simulate physics/limits; one source of truth, clients are views (no divergence) | client computes/asserts vehicle positions | — |
| 2026-07-04 | Loiter = **tangent heading fed into the HSA heading loop** (setpoint regenerated each tick) | one guidance primitive, two features — loiter is "HSA with a recomputed heading" | a separate orbit integrator / L1 vector-field law | racetrack CAP → add legs + a small state machine |
| 2026-07-04 | Ack = **`accepted`/`rejected` + reason string** | covers feasible-vs-violates-a-limit, which is all the demo needs | full MAVLink `MAV_RESULT` enum (6 codes) | need richer status (in-progress, temporarily-rejected) |
| 2026-07-04 | Uplink send seam = **module-singleton socket + `sendCommand()`** called by a cold-lane panel | one datalink, one producer; keeps the rAF hot path + `DeckLayers` untouched | React Context/provider carrying the socket | multiple command producers, or SSR |
| 2026-07-04 | Feasibility at **`tgt_speed`** + HSA-omitted heading **holds current** | check the ring against the speed it'll actually be flown at; heading has no persistent setpoint in loiter, so "unchanged" must mean hold-current | check at instantaneous speed / reuse a stale `tgt_heading` | — |
| 2026-07-04 | **Single `api` container** (sim = asyncio task inside FastAPI) | the GCS↔autopilot separation is already earned at the `VehicleSim` class seam; a 2nd container only adds an IPC bus (teaches message-bussing, not autonomy) | **separate autonomy container** bridged over ZeroMQ/Redis | multiple vehicles · a real SITL binary · autonomy must survive an api restart |
| 2026-07-04 | **Web on the host** in dev; api-only container, web behind a `full-container` profile | WSL2 bind-mount watching is slow (forces polling); host HMR is instant and still reaches the published `:8000` unchanged | containerize the web dev server too | a clone-and-run demo where one `compose up` must serve everything |
| 2026-07-04 | **Single-stage dev image** + bind mount + `uvicorn --reload` | optimizes the edit→reload loop; simplest thing that works | multi-stage baked-in build (builder wheels → slim runtime) | production image → multi-stage + `--workers`, drop `--reload` |
| 2026-07-04 | Hot reload via **native inotify** over the bind mount (no polling flag) | verified edits reload in-container on this WSL2/Docker Desktop; polling would burn CPU for nothing | `WATCHFILES_FORCE_POLLING=true` (belt-and-suspenders) | a setup where edits don't trigger reload → add the flag |
| 2026-07-04 | `requirements.txt` **left unpinned** | matches the project's existing convention; minimal churn | pin exact versions for byte-reproducible rebuilds | image drift / reproducible CI → pin (or add a lockfile) |
| 2026-07-05 | Multi-vehicle = **gateway / fan-in aggregator** (one browser `/ws`, N producers behind it) | one operator link, client stays unchanged, scales to a dynamic fleet; the real GCS pattern | **A:** client opens N sockets (client grows a conn-manager); **C:** message broker (overkill for a few vehicles) | scale to many vehicles / independent consumers / durable replay → the gateway's fan-in is where a broker (C) slots in |
| 2026-07-05 | Producers **dial the gateway and self-register** | dynamic membership with no address config; runtime-spawned sims join the same way | gateway **dials each vehicle** from a config list | — |
| 2026-07-05 | Gateway = **pure relay**: no sim, no clock, only a registry (`vehicleId→conn`) + latest cache | keeps one authoritative sim **per producer**; the invariant becomes "one sim + one clock *per producer*" | gateway holds a **unified sim/clock** (contradicts producers owning their sims) | — |
| 2026-07-05 | `/ingest` **reuses `TelemetryMsg`/`CommandMsg`** + one new **`register`** frame | those types already carry `vehicleId`; one contract end-to-end, only registration is new | a **distinct internal protocol** | drift between internal + client-facing wire → split into a shared package |
| 2026-07-05 | Geozones **move to the gateway** (the GCS owns mission data) | served once per late-joining client; not duplicated per producer nor in the telemetry stream | each producer keeps carrying `GEOZONES` (duplication/conflict) | per-vehicle or mission-defined zones → round-trip via `mission` |
| 2026-07-05 | **One drain task per socket** (per-socket queue; receivers only enqueue) | many producers write one client socket (fan-out) & many clients write one producer socket (fan-in); 2 concurrent `send_text` corrupt the ASGI stream | port `main.py`'s direct-send `sender()+receiver()` into the gateway | — |
| 2026-07-05 | Client outbound = **two-lane buffer** (control never dropped, telemetry drop-oldest) | a back-pressured browser's telemetry burst must not silently evict a pending `commandAck` | **single bounded drop-oldest queue** (evicts the oldest, incl. acks) | — |
| 2026-07-05 | **Backend-only prune** in Phase 10 (killed plane **freezes**; gateway drops it from the registry) | keeps the client byte-identical this phase; new clients' snapshots already omit it | client roster + `removeVehicle` **now** | **Phase 11** — the roster/prune commit removes it from already-open maps |
| 2026-07-05 | Fleet distinctness via **env-parameterized start pose *and* loiter center**; distinct `VEHICLE_ID` per compose service | loiter center is hardcoded, so pose-only producers converge on one ring; defaults reproduce today's single vehicle | hardcoded pose (planes stack) / a shared config | — |
| 2026-07-05 | Producer reconnect catches **`WebSocketException`** + backoff **floor & stable-reset** | a handshake error (not `ConnectionClosed`) mustn't permanently kill the uplink; an accept-then-drop gateway mustn't hot-loop the fleet | catch only `(OSError, ConnectionClosed)`; reset backoff on every connect | — |
